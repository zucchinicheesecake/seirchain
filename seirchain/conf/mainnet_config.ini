[ledger]
max_depth = 8
# Potentially deeper for mainnet scalability (comment moved to its own line)
ledger_version = seir-main-1.0

[mining]
difficulty = 4
# Higher difficulty for mainnet security (comment moved to its own line)
# Real transaction fee (comment moved to its own line)
tx_fee = 0.01

[performance]
num_transactions = 500
# More transactions for larger-scale simulation/mainnet (comment moved to its own line)

[network]
chain_id = seir-mainnet-1
ledger_file = ledger_mainnet.json
# Unique ledger file for mainnet (comment moved to its own line)

